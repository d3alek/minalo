# Проверка на блок
def промените_са_от_различен_източник(блок):
    източници = map(lambda п: п.източник, блок.промени)
    return len(източници) == len(set(източници))

# TODO промените_са_подписани
